# 智能旅行规划助手项目总结

## 1. 项目简介

这是一个基于 FastAPI、LangChain、LangGraph、rag、Vue 3 和MCP高德地图服务构建的智能旅行规划助手。

用户输入城市、日期、出行方式、住宿偏好和旅行偏好后，系统会生成包含景点、路线、天气、餐饮和酒店候选的多日旅行计划。

项目不是简单地调用一次 LLM 生成文字，而是将真实 POI 搜索、地理编码、距离计算、RAG、图片服务、会话编辑和前后端交互组合成完整应用。

## 2. 当前实现的功能

### 2.1 旅行规划

- LangGraph 编排旅行规划流程。
- LangChain 连接兼容 OpenAI API 的 LLM 服务。
- 每天默认安排两个景点。
- 景点全行程去重。
- 根据经纬度计算距离，将相近景点安排在同一天。
- 对规划结果进行 TripPlan 校验。
- 地图编号和每日行程编号保持一致。

### 2.2 高德地图

- 搜索真实景点 POI。
- 搜索真实餐饮 POI。
- 搜索酒店候选。
- 获取天气。
- 对地址进行地理编码。
- 根据经纬度计算景点、餐馆和酒店之间的距离。
- 支持地图展示和路线相关信息。

### 2.3 景点体验

- 景点图片优先使用高德图片。
- 景点支持多图轮播。
- 图片支持点击放大、左右切换和计数显示。
- 景点评点摘要交给 LLM 生成。
- LLM 失败时使用本地摘要兜底。
- 每个景点生成打卡点和游玩建议。

### 2.4 餐饮推荐

- 每天生成早餐、午餐和晚餐。
- 早餐靠近当天第一个景点。
- 午餐靠近当天中间景点。
- 晚餐靠近当天最后一个景点。
- 不同餐次和不同天的餐馆去重。
- 推荐理由中包含距离信息。
- 餐饮图片使用 `category=meal`。
- 餐饮推荐理由支持批量交给 LLM 生成。
- LLM 返回为空、超时或格式错误时使用本地距离理由兜底。

### 2.5 酒店候选

- 酒店模块独立放在行程底部。
- 每次展示最多四个酒店候选。
- 按酒店到全部景点的平均距离排序。
- 展示酒店名称、地址、类型、价格、评分、距离和点评摘要。
- 酒店图片使用独立的 `category=hotel`。
- 酒店图片优先使用高德数据。

### 2.6 RAG

- 当前知识来源是项目内 Markdown 文件。
- 支持家庭出行、低步行强度、美食和预算等知识。
- 使用 Jina Embedding API 生成向量。
- 支持向量检索。
- 向量检索失败时回退到关键词检索。
- 当前不依赖数据库，适合单机学习和项目演示。

### 2.7 会话和 Chat

- 创建旅行 session。
- 保存用户和助手消息。
- 支持局部修改。
- 支持整体重新规划。
- 局部修改无法确定时先追问。
- 整体修改需要用户确认后执行。
- 支持通过 patch operations 修改行程。

## 3. 开发过程中遇到的问题及解决方式

### 3.1 AMAP_API_KEY 已配置但启动时读取不到

问题：

```text
AMAP_API_KEY未配置
```

原因：

- `.env` 所在目录和当前启动目录不一致。
- Python 进程没有读取到预期的环境变量。

解决：

- 从正确的 `backend` 目录启动。
- 设置 `PYTHONPATH=.`。
- 启动前确认环境变量已经进入当前进程。


### 3.2 Jina Embedding 无法连接

问题：

```text
Failed to connect to api.jina.ai port 443
```

原因：

- 浏览器使用了代理，但 PowerShell 和 Python 请求没有自动使用代理。

解决：

```powershell
$env:HTTP_PROXY = "http://127.0.0.1:7897"
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
```

最终 Jina `jina-embeddings-v4` 调用成功，并返回 2048 维向量。

### 3.3 向量检索第一次返回空结果

问题：

```text
assert len(results) == 1
E assert 0 == 1
```

原因：

- fake embedding 与相似度阈值、排序和过滤逻辑没有对齐。

解决：

- 修正向量相似度处理。
- 保留无关问题返回空结果的行为。

最终相关检索测试通过：

```text
8 passed
```

### 3.4 RAG 是否必须使用数据库

问题：

- 是否必须接入数据库才能使用 RAG。

结论：

- 当前 Markdown 文件可以直接作为知识源。
- Jina Embedding 可以为 Markdown 内容生成向量。
- 当前不需要数据库。
- 知识规模扩大后，再考虑向量数据库持久化。

### 3.5 用户问题不在 Markdown 中

问题：

- 用户询问的内容可能不在知识库文件中。

解决：

- RAG 无结果时回退到关键词检索。
- 真实地点、天气和餐饮信息继续使用高德数据。
- LLM 综合用户请求、结构化数据和已有知识。
- 无法确认的内容使用说明或兜底，不伪造真实点评。

### 3.6 LangGraph 规划速度慢

问题：

- 从提交请求到返回完整计划等待时间较长。

原因：

- POI 搜索、天气、地理编码和 LLM 都是外部调用。
- 免费模型响应速度不稳定。
- 多个 POI 需要逐个补充经纬度。

解决：

- LangGraph 并行收集天气、景点和酒店上下文。
- 高德服务增加缓存。
- 复用已初始化的 LLM 实例。
- 餐饮理由使用一次批量 LLM 请求。
- 在保留真实搜索数据的前提下减少重复请求。

### 3.7 天气卡片显示为 0

问题：

- 后端调用高德天气成功，但前端天气显示 0 或相同兜底值。

原因：

- LLM 没有稳定填写 `weather_info`。
- 天气文本没有被可靠解析。

解决：

- 直接解析高德结构化天气结果。
- 后端统一写入 `plan.weather_info`。
- 前端只负责展示后端数据。

### 3.8 景点图片不准确

问题：

- 图片质量不稳定。
- 景点图片混入酒店或餐厅图片。

原因：

- 高德图片字段结构不统一。
- 不同 POI 类型共用了图片逻辑。

解决：

- 景点使用 `category=scenic`。
- 餐饮使用 `category=meal`。
- 酒店使用 `category=hotel`。
- 根据 POI 类型、名称和图片 URL 过滤。
- 排除缩略图和明显不相关图片。

### 3.9 景点重复或最后一天没有景点

问题：

- 三天以上行程容易出现重复。
- 最后一天可能没有景点。
- 每天景点数量不稳定。

原因：

- LLM 可能生成重复景点。
- 高德候选数量不足。
- 路线优化去重后没有足够候选补回。

解决：

- 使用 POI ID 和规范化名称双重去重。
- 扩大候选搜索关键词。
- 规划后检查每日数量。
- 通过候选景点补齐缺失景点。
- 记录 backfill 和每日数量，便于排查。

### 3.10 餐饮推荐重复或距离过远

问题：

- 多个餐次选到同一家餐馆。
- 餐厅距离当天景点较远。

解决：

- 使用 POI ID、名称和地址去重。
- 早餐、午餐、晚餐分别绑定不同景点锚点。
- 按距离选择最近未使用餐馆。
- 推荐理由中保留距离信息。


### 3.11 餐饮推荐理由全部使用兜底

问题：

- 高德餐饮搜索和图片请求都成功，但推荐理由仍是固定模板。

原因：

- 免费模型可能返回空内容。
- 模型可能返回思考过程、嵌套 JSON、数组或普通文本。
- 严格 JSON 解析会失败。

解决：

- 整份行程餐饮理由只调用一次 LLM。
- 限制输出 token。
- 使用 `/no_think` 减少思考内容。
- 兼容普通 JSON、嵌套 JSON、数组、`1=理由` 和 `1:理由`。
- 解析失败时保留本地距离理由。
- 增加“更新数量”和“解析失败内容”的日志。

## 4. 技术架构

```text
Vue 3 + TypeScript + Ant Design Vue
                |
                v
FastAPI
  ├── LangGraph 旅行规划
  ├── LangChain LLM 调用
  ├── 高德 POI / 天气 / 地理编码
  ├── RAG 检索
  ├── Session 会话管理
  └── 图片聚合和类别过滤
                |
                ├── Markdown 知识文件
                ├── Jina Embedding API
                ├── 高德地图 API / MCP
                └── 兼容 OpenAI API 的 LLM 服务
```

## 5. 测试和验证

项目测试覆盖：

- 高德服务和 MCP 工具
- LangGraph 旅行规划
- 景点候选补齐
- 路线优化
- RAG 文档加载和向量检索
- Jina Embedding
- 图片接口
- TripPlan 校验
- Session 会话
- Chat 局部修改
- 整体重新规划
- 景点、餐饮和酒店体验增强

前端构建：

```powershell
cd frontend
npm run build
```

后端启动：

```powershell
cd backend
$env:PYTHONPATH = "."
..\.venv\python.exe -m uvicorn app.api.main:app --host 0.0.0.0 --port 8000
```

## 6. 当前边界

- RAG 当前使用 Markdown，尚未接入持久化向量数据库。
- 高德、Jina 和 LLM 依赖 API Key、外部网络和可能的代理配置。
- 免费 LLM 的速度和输出格式不稳定，因此保留本地兜底。
- 图片质量和数量受高德 POI 图片数据影响。
- Session 当前主要用于本地运行和联调，尚未设计多用户持久化。

## 7. 最终成果

项目已经形成从用户提交需求，到真实 POI 获取、LLM 规划、路线优化、RAG 增强、图片展示、餐饮酒店推荐，再到 Chat 修改和整体重规划的完整闭环。

项目的核心价值不是生成一段旅行文字，而是将 LLM、LangGraph、LangChain、RAG、地图服务和前端交互组合成一个可运行、可测试、可继续扩展的旅行规划应用基础。
